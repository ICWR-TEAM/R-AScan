# Changelog — 2026-06-24

## Documentation workflow

- Added repository-level `AGENTS.md`.
- Established the mandatory first step of reading `NOTE.md` before development work.
- Established the mandatory final step of updating `NOTE.md` and the current date-based changelog.
- Documented the same workflow in `NOTE.md` for project visibility.

## Verification

- Confirmed the repository instructions use the required `docs/changelog/YYYY/MM/DD.md` path.
- Confirmed the initial dated changelog exists at `docs/changelog/2026/06/24.md`.

## Workflow consolidation

- Removed the repository-level `AGENTS.md`.
- Established `NOTE.md` as the sole source for project context and mandatory development workflow.
- Retained the mandatory first step of reading `NOTE.md` and final documentation updates to `NOTE.md` and the dated changelog.

## Verification

- Confirmed `AGENTS.md` no longer exists.
- Confirmed the complete mandatory workflow remains documented in `NOTE.md`.

## Branch configuration

- Renamed the local branch from `pypi-release` to `main`.
- Preserved the existing untracked documentation files during the rename.
- Documented that the remote currently has `origin/pypi-release` but no `origin/main`.

## Verification

- Confirmed the active local branch is `main`.
- Refreshed remote references and confirmed that `origin/main` does not currently exist.

## README renewal

- Reworked installation instructions for PyPI, `pipx`, Windows, virtual environments, and editable source installs.
- Replaced stale `R-AScan.py` commands with the packaged `R-AScan` CLI entry point.
- Corrected the documented Python requirement from 3.8+ to 3.10+.
- Added current usage examples, CLI options, output structure, updater behavior, scanner development guidance, project structure, and development workflow.
- Added explicit authorization and heuristic-result warnings.
- Preserved all six existing badges and the original project screenshot.

## Verification

- Confirmed the README contains all six badge images and the original screenshot URL.
- Confirmed no stale `R-AScan.py` references remain.
- Confirmed Markdown code fences are balanced.
- Confirmed Python source compilation succeeds and `git diff --check` reports no whitespace errors.

## Dynamic scanner core and safety controls

- Added validated host/IP targets with separate custom port and base path handling, including IPv6 formatting.
- Added `ScanConfig`, `ScanContext`, shared HTTP transport, cancellation-aware request budgets, scanner metadata, safety modes, registry filters, normalized results, and versioned reports.
- Added CLI controls for scanner/category selection, exclusions, safety mode, timeout, request budget, proxy, authentication headers, cookies, and TLS verification.
- Changed the default execution policy to exclude intrusive and exploit scanners unless explicitly authorized.
- Serialized legacy plugin execution so nested scanner pools no longer multiply top-level concurrency.
- Removed placeholder authorization and cookie headers.
- Fixed custom-port handling in HTTP smuggling and web extraction.
- Removed the hard-coded stored-XSS mutation against `/post`.
- Downgraded DOM-XSS, access-control, and SSRF heuristics that did not prove exploitation.
- Tightened SSTI output matching and clarified generic rate-limit conclusions.
- Added initial unit tests for targets, scanner selection, modes, cancellation, and request budgets.
- Updated packaging discovery for the new `r_ascan.core` package.

## Verification

- `python3 -m compileall -q r_ascan tests`
- `python3 -m unittest discover -s tests -v` — 10 tests passed.
- `python3 -m r_ascan.app --list-scanners` — all 31 scanners loaded and classified.
- Confirmed URL-form targets are rejected and `host + --port` targets produce normalized report metadata.

## Unified result schema, optimizer, and HTML reporting

- Upgraded reports to schema version 2.0.
- Added a compatibility normalizer that converts heterogeneous legacy module payloads into identical `findings`, `observations`, `errors`, and `summary` fields.
- Added stable finding IDs and common target, endpoint, method, severity, confidence, status, evidence, reproduction, remediation, CWE/OWASP, score, and priority fields.
- Added report-wide finding, severity, status, failure, and risk summaries.
- Replaced the self-trained per-scan random-forest optimizer with deterministic and auditable risk scoring.
- Removed unused NumPy, pandas, and scikit-learn runtime dependencies.
- Added self-contained HTML reporting from the same normalized JSON data, including summary cards, findings, scanner execution state, remediation, and escaped evidence.
- Added `--html` and `--html-output`.

## Verification

- `python3 -m compileall -q r_ascan tests`
- `python3 -m unittest discover -s tests -v` — 15 tests passed.
- Confirmed HTML evidence and titles are escaped.
- Confirmed CLI help includes optimizer and HTML report options.
- Confirmed `git diff --check` reports no whitespace errors.

## Monochrome HTML report

- Reworked the HTML report to use only absolute black (`#000`) and white (`#fff`).
- Set the page, cards, tables, badges, evidence blocks, and print output to a white background with black text and borders.
- Removed colored severity badges, accent colors, gray text, rounded presentation styling, and dark-theme colors.

## Verification

- Extended the HTML renderer test to assert the black/white palette and reject the previous dark-theme colors.

## TLS warning output

- Suppressed `urllib3.exceptions.InsecureRequestWarning` globally for scanner requests that intentionally use `verify=False`.
- Kept all unrelated Python warning categories visible.

## Verification

- Added tests confirming insecure-request warnings are hidden while runtime warnings remain visible.

## PyPI 0.1.0 release preparation

- Bumped the distribution version from `0.0.13` to `0.1.0`.
- Added SPDX license metadata, project URLs, keywords, Python classifiers, and console/security package classifiers.
- Synchronized `requirements.txt` with `pyproject.toml`.
- Added `MANIFEST.in`, expanded build-artifact ignore rules, and documented the release process in `RELEASE.md`.
- Included runtime resources, the license, README, technical notes, and release instructions in the source distribution.
- Removed an embedded credential from the Git remote URL; the exposed credential must be revoked or rotated by its owner.
- Installed Twine locally for release validation.

## Verification

- `python3 -m unittest discover -s tests -v` — 17 tests passed.
- Built `r_ascan-0.1.0-py3-none-any.whl` and `r_ascan-0.1.0.tar.gz`.
- `python3 -m twine check dist/*` passed for both distributions.
- Inspected wheel metadata and confirmed version, dependencies, entry point, package modules, scanners, report renderer, and resource files.
- Installed the wheel into an isolated virtual environment and confirmed all 31 scanners load through `R-AScan --list-scanners`.

## Repeatable custom HTTP headers

- Added repeatable `-H`, `--header`, and `--headers` CLI arguments using `Name: value` syntax.
- Added case-insensitive merging with last-value-wins behavior for duplicate header names.
- Applied dedicated `--authorization` and `--cookie` values after generic headers.
- Propagated merged headers to the shared HTTP transport and all legacy scanners referencing `HTTP_HEADERS`.
- Rejected malformed header names and CR/LF injection attempts.
- Updated the package user-agent version to `R-AScan/0.1.0`.

## Verification

- Added five tests covering repeatable parsing, aliases, duplicate replacement, legacy propagation, malformed headers, and CR/LF rejection.
- Confirmed CLI help displays all custom-header aliases and repeatability behavior.

## Full scanner and HTML defaults

- Changed the default maximum mode from `safe-active` to `exploit`, causing all 31 discovered scanners to run unless filters are supplied.
- Kept `--mode passive`, `--mode safe-active`, and `--mode intrusive` available for intentionally reduced scope.
- Enabled HTML report generation by default beside the JSON report.
- Added `--no-html` to disable automatic HTML output and retained `--html` for command compatibility.
- Added discovered, selected, and skipped scanner counts to terminal/report metadata.
- Added tests for full-scan/HTML defaults and reduced-scope opt-outs.

## Verification

- Confirmed the parser defaults to `mode=exploit` and automatic HTML.
- Confirmed `--mode safe-active --no-html` restores restricted scanning and JSON-only output.

## PyPI release refresh

- Rechecked the public PyPI project before rebuilding.
- Confirmed `0.0.13` remains the latest published release and retained the unused `0.1.0` version.
- Prepared to rebuild `0.1.0` from the latest source, including repeatable headers, all-scanner default execution, and automatic HTML reporting.

## Verification

- `python3 -m unittest discover -s tests -v` — 24 tests passed.
- Rebuilt `dist/r_ascan-0.1.0-py3-none-any.whl` and `dist/r_ascan-0.1.0.tar.gz`.
- `python3 -m twine check dist/*` passed for both artifacts.
- Installed the rebuilt wheel in an isolated virtual environment and confirmed all 31 scanners load.

## PyPI upload diagnostics

- Confirmed the local `0.1.0` wheel and source archive still pass `twine check`.
- Confirmed public PyPI still lists `0.0.13`; the failed upload did not reserve or partially publish `0.1.0`.
- Updated the release command to force `--username __token__`, avoiding local trusted-publishing discovery.
- Enabled verbose upload output so PyPI's actual 400-response detail is visible.

## PyPI 0.1.1 release

- PyPI reported that the `0.1.0` filenames had previously been uploaded and deleted.
- Advanced the package version and HTTP user-agent from `0.1.0` to `0.1.1`.
- Recreated `RELEASE.md` with immutable-version guidance and explicit `0.1.1` upload commands.

## PyPI 0.0.15 release selection

- Changed the pending release version from `0.1.1` to maintainer-selected `0.0.15`.
- Updated package metadata, HTTP user-agent, release instructions, and installation examples to `0.0.15`.
