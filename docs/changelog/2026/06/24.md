# Changelog — 2026-06-24

## Documentation workflow

- Added repository-level `AGENTS.md`.
- Established the mandatory first step of reading `NOTE.md` before development work.
- Established the mandatory final step of updating `NOTE.md` and the current date-based changelog.
- Documented the same workflow in `NOTE.md` for project visibility.

## Verification

- Confirmed the repository instructions use the required `docs/changelog/YYYY/MM/DD.md` path.
- Confirmed the initial dated changelog exists at `docs/changelog/2026/06/24.md`.

## Workflow consolidation

- Removed the repository-level `AGENTS.md`.
- Established `NOTE.md` as the sole source for project context and mandatory development workflow.
- Retained the mandatory first step of reading `NOTE.md` and final documentation updates to `NOTE.md` and the dated changelog.

## Verification

- Confirmed `AGENTS.md` no longer exists.
- Confirmed the complete mandatory workflow remains documented in `NOTE.md`.

## Branch configuration

- Renamed the local branch from `pypi-release` to `main`.
- Preserved the existing untracked documentation files during the rename.
- Documented that the remote currently has `origin/pypi-release` but no `origin/main`.

## Verification

- Confirmed the active local branch is `main`.
- Refreshed remote references and confirmed that `origin/main` does not currently exist.
